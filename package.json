{
  "name": "vm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client": "npm run dev --prefix front",
    "server": "npm run start:dev --prefix back",
    "build": "concurrently \"npm run build --prefix front\" \"npm run build --prefix back\"",
    "pusk": "concurrently \"npm run start --prefix front\" \"npm run start --prefix back\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}